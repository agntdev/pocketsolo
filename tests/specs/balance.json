[
  {
    "name": "/balance shows account balance",
    "steps": [
      {
        "send": { "text": "/balance" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Couldn't connect to Pocket Option. Check your credentials in environment variables and try again."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "balance button shows account balance",
    "steps": [
      {
        "send": { "callback": "balance:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Couldn't connect to Pocket Option. Check your credentials in environment variables and try again."
            }
          }
        ]
      }
    ]
  }
]
